<!DOCTYPE html>
<html>
  <head>
    <title>FilmTraker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>


  <div class="container">
    <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
      <div class="col-md-3 mb-2 mb-md-0">
        <a href="/" class="d-inline-flex link-body-emphasis text-decoration-none">
          <svg class="bi" width="10" height="25" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"/></svg>
        </a> Films Catalog</a>
      </div>

      <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
      <li class="nav-item"><%= link_to('Home', root_path, class: 'nav-link') %></li>
      <li class="nav-item" ><%= link_to('Films', film_path, class: 'nav-link') %></li>
      
      </ul>

      <div class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
      <%= link_to(" #{current_user&.email}", destroy_user_session_path, method: :delete, class: 'nav-link') %>

      
      </div>
    </header>
  </div>

  <div class="container">
    <%= yield %>
    
    </div>
  </body>
</html>
