<%= form_for(film.comments.new) do |f| %>
    <%= f.label :body, value: 'New comment', class: 'form-label' %>
    <%= f.text_area :body, class: "form-control " %>
    <%= f.hidden_field :film_id, value: film.id %>
    <%= f.submit class: 'btn btn-outline-success mt-3' %>
  <% end %>
  